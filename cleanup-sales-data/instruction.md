# Task Overview

You are tasked with fixing a memory leak in the provided Python web service.

## Instructions

1. Inspect the source code located at `/app/main.py`.
2. Locate the memory leak occurring during user session initialization.
3. Patch the issue without breaking existing unit tests.
4. Ensure all tests pass by running `pytest` inside `/app`.

## Constraints

* Do not modify the test suite in `/app/tests/`.
* Do not introduce external dependencies outside `requirements.txt`.
